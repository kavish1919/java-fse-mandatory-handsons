<!--
    logback.xml – Logback configuration for the SLF4J Logging Exercise.

    Placed in src/main/resources so Logback auto-discovers it on the classpath.

    Appenders configured:
      1. CONSOLE  – prints to stdout with a timestamp + level + logger + message pattern
      2. FILE     – writes the same output to logs/application.log (rolling daily)

    Root logger level: TRACE (allows all 5 levels to be observed in the demo)
-->
<configuration>

    <!-- ======================================================================
         CONSOLE Appender – writes to System.out
    ====================================================================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!--
                Pattern tokens:
                  %d{yyyy-MM-dd HH:mm:ss.SSS}  – timestamp
                  [%-5level]                    – log level, left-padded to 5 chars
                  %logger{36}                   – logger name, truncated to 36 chars
                  - %msg                        – the log message
                  %n                            – newline
            -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ======================================================================
         FILE Appender – writes to logs/application.log with daily rolling
    ====================================================================== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Roll over daily; keep last 7 days -->
            <fileNamePattern>logs/application.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ======================================================================
         Root Logger – set to TRACE so all five levels are visible
    ====================================================================== -->
    <root level="TRACE">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
